# Relationship Inference Plan

This file documents the next bridge step for lineage rendering.

The goal is to compute visible kinship labels from existing lineage tables and imported GEDCOM data, then expose those labels in the family tree UI without changing production runtime.
